version: '3.8'

services:
  postgresdb:
    image: postgres:13.4-alpine
    container_name: postgresql
    environment:
      POSTGRES_DB: evamodulo6
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"